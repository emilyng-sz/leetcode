from typing import List
# https://leetcode.com/problems/maximal-rectangle/
class Solution:
    def maximalRectangle(self, matrix: List[List[str]]) -> int:
        # brute force:

        # get all coordinates of 1s
        # iterate through all quadruples of 1s (very un-ideal because the insides must also all be 1s)

        # Graph 
        
        # use idea of Graph, because tracking if I have traversed the part of graph is v helpful.. 
    
        # Recursion??

        
        pass